# gymbro requirements
#
# MediaPipe needs Python 3.9-3.12. On 3.13+ it has no wheel yet; use 3.12.

# --- core (needed for everything) ---
numpy>=1.24
PyYAML>=6.0

# --- vision (needed for `gymbro train`) ---
opencv-python>=4.8
mediapipe>=0.10.9        # BlazePose GHUM: metric 3D world landmarks

# --- optional ---
pyttsx3>=2.90            # spoken rep counts and cues

# --- development ---
pytest>=7.4

# GPU note: MediaPipe's Python wheels run the pose graph on CPU. On an RTX 4060
# that is still comfortably real-time at model_complexity=2 (the heavy model),
# which is what gymbro uses by default. If you later want GPU inference, the
# path is ONNX Runtime with the CUDA provider and an RTMPose or ViTPose model
# behind the same PoseBackend protocol in gymbro/pose/backends.py -- nothing
# downstream of the backend needs to change.
